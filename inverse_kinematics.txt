--@name Untitled
--@author Lil'Tugboat
--@server

local Segment = {
    Pos = Vector(0)
    PrevPos = Vector(0)
}

local function Segment:init(pos)
    local self = setmetatable({}, Segment)
    self.position = pos
    self.prevPos = pos
    return self
end

local function Segment:update(target)
    local dpos = 
    local dx = target.x
    
--[[
Starfall Scripting Environment

StarfallEx Addon: https://github.com/thegrb93/StarfallEx
Documentation: http://thegrb93.github.io/StarfallEx

This default code can be edited via the 'default.txt' file
]]
